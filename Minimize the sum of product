class Solution {

    public long minValue(long a[], long b[], long n) 
    {
        // Your code goes here
        long sum=0;
        Arrays.sort(a); 
        Arrays.sort(b); 
        for(int i=0;i<n;i++)
        {
            sum=sum+a[i]*b[b.length-1-i];
        }
        return sum;
    }
}
